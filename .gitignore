*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
*.apk
*.aab
*.aar
*.ap_
*.dex
*.class
bin/
gen/
out/
release/
.kotlin/

# Gradle
**/build/
!src/**/build/
gradle-app.setting
.gradletasknamecache

# Test reports
/**/reports/
/**/test-results/

# Native libraries built locally
/protocols/**/src/main/jniLibs/*/
/protocols/**/go-build/

# gomobile umbrella artifacts — regenerated by scripts/build-native.sh,
# no need to commit (the .aar itself is already matched by *.aar above).
/protocols/awg/libs/*.jar

# Outputs of scripts/build-native.sh explode step — regenerated by CI,
# never commit the umbrella bundle pieces.
/app/libs/*.jar
/app/src/main/jniLibs/

# VSCode user-local
.vscode/*.log
.vscode/ipch/

# OS
Thumbs.db

# Release signing — NEVER commit
/keystore.properties
*.jks
*.keystore
*.jks.bak
*.keystore.bak

# Upstream Go clones produced by scripts/build-native.sh
/vendor/

# Scratch file used for multi-line commit messages on Windows
/.commitmsg

# Claude Code project-local data — settings, memory, task locks
/.claude/
