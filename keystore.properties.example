# Copy to keystore.properties (gitignored) and fill in.
# CI overrides these via PAYKEYFEAR_KEYSTORE_* environment variables.
storeFile=release.jks
storePassword=CHANGE_ME
keyAlias=paykeyfear
keyPassword=CHANGE_ME
